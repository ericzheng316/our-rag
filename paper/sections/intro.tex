% ============================================================
% Section 1 — Introduction (v3, 2026-09-16: 主方法改为查询级证据信用)
% v2 (2026-08-28, 广播覆盖项为主方法) 的第 4-5 段与贡献列表以注释保留在文末。
% 数据: HANDOFF §22 (K=8 修正), §29/§30 (查询级信用 seed 1), §16/§20 (开放域三对),
%       §31-§37 (开放续训), §33 (训练后 K=8)
% ============================================================

\section{Introduction}
\label{sec:intro}

  Multi-hop question answering requires an agent to acquire evidence
  sequentially: each retrieval exposes an intermediate entity that the next
  query depends on. Reinforcement learning has become the standard way to
  train such search agents---the policy interleaves reasoning, search, and
  answering over several turns, optimized by a critic-free policy
  gradient whose baseline is the mean reward of the $K$ rollouts drawn
  for the same question---group-relative advantage estimation, as in
  GRPO and leave-one-out
  REINFORCE~\citep{shao2024deepseekmath,ahmadian2024rloo,kool2019buy}---
  against a terminal reward for answer
  correctness~\citep{jin2025searchr1,chen2025research,song2025r1searcher,
  sun2026zerosearch}. This recipe improves aggregate accuracy, but its gains
  concentrate on shallow questions; on the deepest chains, where sequential
  evidence acquisition matters most, improvements remain modest.

  The standard diagnosis is temporal~\citep{zheng-etal-2025-stepsearch}:
  rewards are sparse along the trajectory, so intermediate progress goes
  uncredited. We argue for a
  different, \emph{group-level} diagnosis. Under group-relative advantage
  estimation, the policy gradient exists only where the $K$ rollouts of
  the same question differ in return; identical returns cancel exactly.
  On hard questions the terminal reward routinely fails to differ: every
  rollout is wrong, or every rollout is right. We call such a group
  \emph{answer-tied}. In deep search this is the dominant case: at the
  SFT initialization, 56--71\% of rollout groups are answer-tied at every
  depth. Depth changes which kind of tie dominates. At two hops, 28\% of
  groups are tied because every rollout is right; at four hops that share
  falls to 4\%, and 57\% of all groups are tied because every rollout is
  wrong.\footnote{Measured at the SFT initialization with $K{=}8$ rollouts
  at the training temperature; protocol in \S\ref{sec:mechanism}.} In an
  answer-tied group the terminal reward is the same for all $K$ rollouts
  and contributes nothing to any advantage: a rollout that retrieved half
  of the gold evidence chain and one that retrieved none receive identical
  reward. The ordering that would separate them---how much of the
  evidence each rollout reached---is the signal this paper adds. What
  remains in an all-wrong group is the cost terms---search cost, token
  cost, format penalties---so the rollout that searched least ranks
  highest. On the questions where the agent most needs to learn to keep
  searching, the training signal rewards stopping early.

  This failure mode has recently been named in mathematical
  RLVR---\emph{advantage collapse}~\citep{he2026collapse}---and existing
  mitigations fall into three families. Discarding methods drop or skip
  uniform-outcome groups by dynamic sampling or
  filtering~\citep{yu2025dapo,greso2025,xu2026downsampling}, recovering
  compute but abandoning exactly the hardest questions. Synthetic-advantage
  methods inject virtual reward samples so that collapsed groups still
  produce a learning signal~\citep{he2026collapse}, but the signal is
  content-free: the ordering inside a tied group remains arbitrary.
  Structural methods re-baseline the comparison itself, stratifying
  advantages by trajectory structure~\citep{zhu2025grpo} or comparing
  along shared tree prefixes~\citep{Ji2026tree}; this repairs
  cross-question fairness but leaves a fully tied group as uninformative as
  before. Leaving such groups to chance is not neutral: in GRPO training,
  whatever residual signal occupies an all-failure group can come to govern
  the learned policy outright~\citep{wang2026darkroom}. Tied groups should
  be neither thrown away nor filled with noise; what they need is an
  ordering that \emph{means} something.

  Standard multi-hop training sets already contain the information that
  breaks these ties: gold supporting documents~\citep{trived2022musique,
  yang2018hotpotqa}. Where this information enters the estimator decides
  what the policy learns from it. Added to the trajectory return as an
  evidence-coverage term, it reorders whole trajectories: the estimator
  computes one advantage per rollout and broadcasts it to every turn, so
  the search step that reached new evidence and the answer step that
  followed receive the same sign. This placement teaches the policy
  \emph{when to answer}---withhold until the evidence set is
  complete---a habit that pays inside the training environment and costs
  outside it (\S\ref{sec:mechanism}, \S\ref{sec:transfer}). We therefore
  assign the credit at the query step. At each search turn we sample $M$
  additional queries from the current policy at the same state, retrieve
  for each against the same environment, and credit the executed query by
  its gold-evidence gain relative to the mean gain of the samples: a
  state-conditional baseline estimated from the policy's own action
  distribution, as in the vine estimator of \citet{schulman2015trpo}. The credit needs the dataset's gold annotations and the
  retriever, nothing else, and is used only during training. Answer turns
  receive the outcome reward alone. In an answer-tied group the credit is
  non-zero wherever the executed query and its alternatives reach
  different evidence, which is where the tie is broken.

  Our experiments on MuSiQue with a Qwen3.5-9B policy yield three
  findings. \textbf{First}, query-level credit improves deep search in
  the training environment: on the closed-pool development set it
  reaches 51.4 exact match against 47.6 for outcome-only training and
  47.5 for the trajectory-level coverage term from the same
  initialization and seed ($z=4.7$ and $4.9$), with 4-hop EM 40.0
  against 31.1 ($z=4.1$), higher final evidence coverage, and 0.6 fewer
  search calls per question. \textbf{Second}, a group-level audit
  locates the gain: at the training start, 84\% of 4-hop answer-tied
  groups carry coverage variation the answer reward cannot see, adding
  coverage expands the share of 4-hop groups with an informative
  ordering from 38\% to 89\%, and on the trained policy the share of
  4-hop groups in which every rollout is wrong falls from 42.9\% to
  37.3\% ($z=2.7$). \textbf{Third}, on an open 21M-passage corpus the
  trajectory-level term is a training-environment habit---over three
  seed-matched pairs it loses 2.75 EM on 2WikiMultihopQA ($z=-6.4$),
  entirely on questions answered from incomplete evidence---whereas
  query-level credit carries no such penalty, and continued training
  against the open retriever yields the best open-corpus results of
  this study (28.7 EM on MuSiQue, 47.0 on 2WikiMultihopQA), the credit
  adding $+2.7$ on 2WikiMultihopQA over the same continued training
  without it ($z=3.7$).
  % 2026-09-02 用户决议: 安置研究(涉及自训 judge 模型)暂不进 intro 发现列表,
  % 需要时取消注释并把上文 "three findings" 改回 "four"。
  % \textbf{Fourth}, the same signal that helps as a training reward
  % and as an offline audit yields no reliable gain as a runtime controller
  % or as an injected observation: evidence supervision is
  % placement-sensitive.

  Our contributions:
  \begin{itemize}\setlength\itemsep{2pt}
  \item \textbf{A group-level diagnosis.} Answer-tied groups dominate
    deep-search RL (56--71\% of groups), shift with depth from all-right
    ties to all-wrong ties (57\% of 4-hop groups), and carry a
    cost-driven residual ordering that favors the rollout that searched
    least---an account of deep-hop stagnation that
    temporal sparsity does not capture, complementing advantage-collapse
    analyses in mathematical RLVR~\citep{yu2025dapo,he2026collapse}
    (\S\ref{sec:starvation}).
  \item \textbf{Query-level evidence credit.} A turn-level credit for
    search queries, computed against a state-conditional counterfactual
    baseline from the policy's own samples and the dataset's gold
    annotations, with the trajectory-level coverage term as the
    controlled contrast that isolates where the signal must enter
    (\S\ref{sec:method}, \S\ref{sec:mechanism}).
  \item \textbf{Deep-hop gains, and the boundary of the training
    environment.} $+3.8$ EM and $+8.9$ 4-hop EM over outcome-only
    training in the closed pool; no penalty on the open corpus, where the
    trajectory-level term loses up to 5.9 points at four hops; and the
    best open-corpus results after continued training against the open
    retriever (\S\ref{sec:results}, \S\ref{sec:transfer}).
  \item \textbf{A signal-placement study.} The same evidence signal helps
    in training and offline audit but not as runtime control or injected
    observation (\S\ref{sec:placement}).
  \end{itemize}

% ---------------------------------------------------------------
% v2 原文(2026-08-28 至 09-03, 广播覆盖项为主方法), 供回溯:
% 第 4 段: "Standard multi-hop training sets already contain the information
% that breaks these ties: gold supporting documents. We add to the trajectory
% return a \emph{completion-gated, first-hit evidence-coverage} term. We
% compute coverage as the set overlap between retrieved evidence and training
% annotations; the signal is deterministic and used only during training. A
% rollout is credited once for each gold document it retrieves, normalized
% by the size of the gold set, and receives the credit only if it commits to
% an answer. Under a trajectory-level group-relative estimator the term acts
% on whole-trajectory returns; its effect---simpler than step-level process
% supervision---is to \emph{restore within-group ranking}. Uniformly failing
% rollouts become ordered by how much of the required evidence they actually
% reached, rather than by how little they spent failing."
% 发现一: "coverage shaping improves deep-hop accuracy: pooled over four
% matched pairs of runs, 4-hop exact match improves by $+3.6$ points over the
% outcome-only counterpart ($z=3.62$), positive in three of the four pairs,
% with overall EM up $+0.9$ and search calls per question unchanged."
% 发现二: "... 89\% of 4-hop answer-tied groups carry coverage variation ...
% from 38\% to 93\% ..." (K=8 分隔符 bug 修正前的数字)
% 发现三: "the gains transfer zero-shot: $+2.05$ EM on 2WikiMultihopQA
% ($+4.52$ at four hops) and $+4.75$ on HotpotQA comparison questions ..."
% (单对结论, 三对配对撤回)
% 贡献二: "\textbf{A trajectory-ranking repair.} Completion-gated first-hit
% coverage, deterministic and used only during training, restores
% within-group ordering where prior mitigations discard, synthesize, or
% re-baseline."
% 贡献三: "\textbf{Deep-hop gains that transfer.} 4-hop improvements over
% matched outcome-only runs at unchanged search cost, and zero-shot transfer
% to 2WikiMultihopQA and HotpotQA comparison questions."
% ---------------------------------------------------------------
