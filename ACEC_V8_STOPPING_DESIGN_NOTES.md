# 停机决策的设计论证(论文素材,2026-08-07)

写论文时的直接引用材料。核心主张:**多跳检索 agent 的停机不是提示工程问题,
而是 MDP 状态完备性问题**;以及 **SFT 与 RL 在行为习得上有不可互换的分工**。

## 1. 实证起点:53% 的轨迹撞上 turn 上限

SFT v2(122B 蒸馏,含 55% 自然改写示范)在 MuSiQue 闭池 dev 360 题上:

| | SFT v1 | SFT v2 |
|---|---|---|
| 学生自然改写率 | 0% | 84.4% |
| 撞 turn 上限(8)且未作答 | 0% | **53%**(192/360) |
| 未撞上限子集的 EM | 0.192(全体) | **0.482** |
| 2hop / 3hop / 4hop(未撞上限子集) | 0.283 / 0.200 / 0.156 | 0.683 / 0.500 / 0.316 |

能力显著提升(未撞上限子集全线碾压),但一半轨迹陷入无限改写。
**关键观察:它不是"选择了放弃",而是从未做过停机这个决策。**

## 2. 论证:剩余预算必须进入观测(状态完备性)

有限视界 MDP 的最优策略是**非平稳的**——必然依赖剩余步数 t
(Puterman; Bertsekas)。形式化:π\*: (o, t_remaining) → a。当观测 o
不含 t_remaining 时,可表示策略类 Π = {π: o → a} **不包含 π\***。
这不是"学不好",是最优解不在搜索空间内。

[Pardo et al., ICML 2018, "Time Limits in Reinforcement Learning"]
(https://proceedings.mlr.press/v80/pardo18a.html) 给出了标准处理:当
**因时间上限终止属于环境的一部分**(其 case (i))时,剩余时间必须进入
agent 输入,否则**违反 Markov 性质并造成 state aliasing**。

我们属于 case (i):`max_turns` 是任务契约的一部分(推理期同样存在),
不是仅训练期的便利截断。

**state aliasing 的实证**:模型在第 2 turn 与第 8 turn 面对同构局面做
同一决策(继续改写)——在它的观测里这两个状态完全相同。53% 的撞墙率
是这一混叠的直接后果。

**对"这只是 prompt engineering"的判据式反驳**:
- prompt engineering = 状态空间不变,改变同一状态下的诱导偏好;
- 状态补全 = 改变状态空间,使最优策略从不可表示变为可表示。
类比:给下棋 agent 时钟读数不是提示技巧,不给才是 bug。

## 3. 正确的形式化:最优停时(optimal stopping)

每 turn 的决策为:停(作答,得 p_t)vs 继续(付 c_r,进入 s_{t+1})。
最优规则(Snell envelope):

    停 ⟺ p_t ≥ E[V(s_{t+1})] − c_r

其中 `E[V(s_{t+1})] − p_t` 正是**信息价值(VOI)**。ACEC 的 slot 覆盖
后验即为估计该量的状态表示 —— **VOI 停机是 ACEC 的原始设计意图**。

## 4. 机制价值依赖于策略能力阈值(论文级发现)

早期证伪程序判定 VOI-gate(Mechanism C)**无效**(τ-dial inert,
1003/1024 轨迹相同)。但那是在**从不改写、平均 3 步收敛**的策略上测的
——**停机困境不存在,机制无用武之地**。SFT v2 赋予自我修正能力后,停机
才成为真问题(53% 撞墙)。

**结论:机制的可测价值以策略能力达到某阈值为前提。** 这既是对"早期消融
显示无效"的诚实交代,也是一个可推广的方法论结论:能力先行,机制随后;
在能力不足的策略上做机制消融,阴性结果不可外推。

## 5. SFT 与 RL 的分工原则(两个互补的实证)

| 行为 | 能否由 RL 从零学到 | 能否由 SFT 教会 | 实证 |
|---|---|---|---|
| 同 slot 改写 | **否**——先验为零,温度采不出(引导 rollout 200 题 0 条) | **是**——teacher 示范后学生 84% | 2026-08-07 |
| 预算感知停机 | **是**——状态完备后可由回报学到 | **否**——teacher 从未撞上限(max 7 / 预算 8),示范数据里不存在该行为 | 本文档 |

**原则:RL 只能强化行为分布中已存在的模式;SFT 只能教示范中出现过的
情境。二者的空缺互补,不可互换。**

## 6. 设计层次与预注册消融

- **第 1 层(必需,correctness fix)**:剩余预算进入观测;
- **第 2 层(形式化)**:anytime answer(每 turn 产出当前最佳答案 → p_t
  可观测、回报稠密化)/ 显式 COMMIT 动作 / turn 级 credit(后缀回报差分);
- **第 3 层(本文机制)**:belief 驱动的 VOI 停机。

预注册三臂:A 无预算(对照,预期 state aliasing)/ B 预算可见 / C = B +
belief-VOI 信号。判据:放弃率与 EM 的联合改善。第 1 层由此成为消融中的
**基线控制条件**,而非孤立的技巧。

## 7. 相关的 reward 设计缺陷(同期修复,须在论文中交代)

修复前 `hit_turn_limit` 无 reward 分支,且过程覆盖分与检索次数正相关:

| 轨迹 | 修复前净回报 | 修复后 |
|---|---|---|
| 打满、全覆盖、不作答 | **+0.26** | −0.34 |
| 3 检索、半覆盖、答错 | +0.135 | +0.135 |

即**放弃作答的收益曾是尝试作答的两倍**——无限改写行为被正向奖励。
修复:`--turn_limit_penalty 0.3`(重于格式错 0.2)且放弃轨迹不发过程分。

数学注记:惩罚的**符号**而非大小是关键。作答期望回报 = p ≥ 0,放弃 =
−δ < 0,故理性策略下放弃率应为 0,与 δ 大小无关。残留放弃率因此是
**策略非理性(状态混叠 / credit assignment 跨度)的度量**,而非定价问题
——这使 `giv=` 成为一个干净的诊断指标。
