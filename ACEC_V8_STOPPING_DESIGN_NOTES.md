# 停机决策的设计论证(论文素材,2026-08-07)

写论文时的直接引用材料。核心主张:**多跳检索 agent 的停机不是提示工程问题,
而是 MDP 状态完备性问题**;以及 **SFT 与 RL 在行为习得上有不可互换的分工**。

## 1. 实证起点:53% 的轨迹撞上 turn 上限

SFT v2(122B 蒸馏,含 55% 自然改写示范)在 MuSiQue 闭池 dev 360 题上:

| | SFT v1 | SFT v2 |
|---|---|---|
| 学生自然改写率 | 0% | 84.4% |
| 撞 turn 上限(8)且未作答 | 0% | **53%**(192/360) |
| 未撞上限子集的 EM | 0.192(全体) | **0.482** |
| 2hop / 3hop / 4hop(未撞上限子集) | 0.283 / 0.200 / 0.156 | 0.683 / 0.500 / 0.316 |

能力显著提升(未撞上限子集全线碾压),但一半轨迹陷入无限改写。
**关键观察:它不是"选择了放弃",而是从未做过停机这个决策。**

## 2. 论证:剩余预算必须进入观测(状态完备性)

有限视界 MDP 的最优策略是**非平稳的**——必然依赖剩余步数 t
(Puterman; Bertsekas)。形式化:π\*: (o, t_remaining) → a。当观测 o
不含 t_remaining 时,可表示策略类 Π = {π: o → a} **不包含 π\***。
这不是"学不好",是最优解不在搜索空间内。

[Pardo et al., ICML 2018, "Time Limits in Reinforcement Learning"]
(https://proceedings.mlr.press/v80/pardo18a.html) 给出了标准处理:当
**因时间上限终止属于环境的一部分**(其 case (i))时,剩余时间必须进入
agent 输入,否则**违反 Markov 性质并造成 state aliasing**。

我们属于 case (i):`max_turns` 是任务契约的一部分(推理期同样存在),
不是仅训练期的便利截断。

**state aliasing 的实证**:模型在第 2 turn 与第 8 turn 面对同构局面做
同一决策(继续改写)——在它的观测里这两个状态完全相同。53% 的撞墙率
是这一混叠的直接后果。

**对"这只是 prompt engineering"的判据式反驳**:
- prompt engineering = 状态空间不变,改变同一状态下的诱导偏好;
- 状态补全 = 改变状态空间,使最优策略从不可表示变为可表示。
类比:给下棋 agent 时钟读数不是提示技巧,不给才是 bug。

## 3. 正确的形式化:最优停时(optimal stopping)

每 turn 的决策为:停(作答,得 p_t)vs 继续(付 c_r,进入 s_{t+1})。
最优规则(Snell envelope):

    停 ⟺ p_t ≥ E[V(s_{t+1})] − c_r

其中 `E[V(s_{t+1})] − p_t` 正是**信息价值(VOI)**。ACEC 的 slot 覆盖
后验即为估计该量的状态表示 —— **VOI 停机是 ACEC 的原始设计意图**。

## 4. 机制价值依赖于策略能力阈值(论文级发现)

早期证伪程序判定 VOI-gate(Mechanism C)**无效**(τ-dial inert,
1003/1024 轨迹相同)。但那是在**从不改写、平均 3 步收敛**的策略上测的
——**停机困境不存在,机制无用武之地**。SFT v2 赋予自我修正能力后,停机
才成为真问题(53% 撞墙)。

**结论:机制的可测价值以策略能力达到某阈值为前提。** 这既是对"早期消融
显示无效"的诚实交代,也是一个可推广的方法论结论:能力先行,机制随后;
在能力不足的策略上做机制消融,阴性结果不可外推。

## 5. SFT 与 RL 的分工原则(两个互补的实证)

| 行为 | 能否由 RL 从零学到 | 能否由 SFT 教会 | 实证 |
|---|---|---|---|
| 同 slot 改写 | **否**——先验为零,温度采不出(引导 rollout 200 题 0 条) | **是**——teacher 示范后学生 84% | 2026-08-07 |
| 预算感知停机 | **是**——状态完备后可由回报学到 | **否**——teacher 从未撞上限(max 7 / 预算 8),示范数据里不存在该行为 | 本文档 |

**原则:RL 只能强化行为分布中已存在的模式;SFT 只能教示范中出现过的
情境。二者的空缺互补,不可互换。**

## 5b. 第 1 层的零训练实证(2026-08-07 冒烟,SFT v2,120 题,贪心)

| 臂 | 协议错 | 撞上限未答 | 平均检索 | EM |
|---|---|---|---|---|
| A 无预算 | 3 | **47.5%** | 6.08 | 0.275 |
| B 预算可见 | 3(相同) | **8.3%** | 5.72 | **0.333** |

**零训练**下撞上限率降 82%、EM +5.8pp,协议合规不变(无格式 OOD 代价)。

**这修正了 §5 表格中"预算感知停机 SFT 教不会"一栏的推论**:该行为确实不在
teacher 示范里,但 9B 的指令跟随足以**零样本**使用该信号(系统提示声明了
"reaches 0 you must answer")。故更精确的表述是:*该行为无法从示范中习得,
但可由显式的状态信号直接触发* —— 这进一步支持"状态完备性"而非"训练不足"
的诊断:模型一直有能力停机,只是看不到该停了。

## 6. 设计层次与预注册消融

- **第 1 层(必需,correctness fix)**:剩余预算进入观测;
- **第 2 层(形式化)**:anytime answer(每 turn 产出当前最佳答案 → p_t
  可观测、回报稠密化)/ 显式 COMMIT 动作 / turn 级 credit(后缀回报差分);
- **第 3 层(本文机制)**:belief 驱动的 VOI 停机。

预注册三臂:A 无预算(对照,预期 state aliasing)/ B 预算可见 / C = B +
belief-VOI 信号。判据:放弃率与 EM 的联合改善。第 1 层由此成为消融中的
**基线控制条件**,而非孤立的技巧。

## 7. 相关的 reward 设计缺陷(同期修复,须在论文中交代)

修复前 `hit_turn_limit` 无 reward 分支,且过程覆盖分与检索次数正相关:

| 轨迹 | 修复前净回报 | 修复后 |
|---|---|---|
| 打满、全覆盖、不作答 | **+0.26** | −0.34 |
| 3 检索、半覆盖、答错 | +0.135 | +0.135 |

即**放弃作答的收益曾是尝试作答的两倍**——无限改写行为被正向奖励。
修复:`--turn_limit_penalty 0.3`(重于格式错 0.2)且放弃轨迹不发过程分。

数学注记:惩罚的**符号**而非大小是关键。作答期望回报 = p ≥ 0,放弃 =
−δ < 0,故理性策略下放弃率应为 0,与 δ 大小无关。残留放弃率因此是
**策略非理性(状态混叠 / credit assignment 跨度)的度量**,而非定价问题
——这使 `giv=` 成为一个干净的诊断指标。

## 8. 开卷蒸馏的数据卫生:三类污染与两级防线(2026-08-08 全量审计)

开卷交互式蒸馏(teacher 上下文含全池小抄,落盘时剥离)天然带一个风险:
teacher 的 think 可能引用学生看不见的信息。对 3,889 段 think 的全量审计
(上文外新词计数 + 人工分诊)发现三类污染,处置各不相同:

1. **meta 引用**("Reference 8"、"the provided text"):把小抄的存在本身
   说出来。学生模仿后凭空捏造文档编号。→ 措辞禁词族,命中即弃整条。
2. **实质开卷泄漏**(think 里复述未检索到的段落内容):学生学到"凭空变出
   证据"。→ 新词手术:上文外新词≥5(剔除元词汇白名单)的 think 段删除,
   轨迹保留——think 是协议可选项,删除不破坏结构,产量损失比弃整条小一个
   量级。
3. **参数知识独白**(teacher 的世界知识以确信口吻出现在 think):122B 的
   断言多半为真,但 9B 模仿的是"确信口吻"而非知识本身,产生它随后据以行动
   的假断言。→ 与第 2 类同一手术覆盖(高新词密度是共同签名)。

同时假阳性要防:失败反思型 think 的"新词"是 refine/broad/prioritize 等
元词汇——这是最有教学价值的段落(改写演示),白名单剔除后才能下刀。

量化:v2.1 语料 5.6% 的 think 段含 ≥4 新词;v2.2(禁词族扩充 + 手术)
后高危段复检为 0,代价 3,015→2,976 条(-1.3%)与 253 段 think(-6.5%)。

防线分级:语料过滤是第一级,训后学生行为审计(dev rollout 全量 think 过
禁词表 + 人工抽查)是真正的验收门——过滤只能拦已知模式,学生泛化出的新
幻觉形态只有行为审计能暴露。

## 9. 自由通道通胀:两级定价失败与梯度屏蔽(2026-08-09,论文级负结果)

轨迹级优势广播下,parser 不解析的 token(think/裸文本)白拿正梯度、零成
本,概率质量单调通胀直至复读失控。四次段 1 启动的完整证据链:

| 启动 | 反制手段 | 崩塌位 | 病理形态 |
|---|---|---|---|
| 一启(320) | 无 | ep20 | 单 think 膨胀截断 |
| 二启(448) | 上限+降 lr | ep27 | think 逐字复读 + 伪造 result |
| 三启 | + c_tok=1e-4 | ep26 判据 | ftk 0.4→17.3 五连几何 |
| 四启 | + c_tok=1e-3 | ep34 | ftk 0.2→56.7,inv 0.12 |

**定价失败的定量机理**:RLOO 的反压来自同组 rollout 的成本差,而通胀的
推力来自完整轨迹优势(~0.3–0.5)对每个 token 的广播。成本差要与之抗衡,
需要 c_tok×Δftk ≈ 优势量级——在可容忍价格下均衡点位于复读失控区之上。
1e-3(448 复读付 0.45)只把崩塌从 ep26 推迟到 ep34,增速曲线全程未弯头。
教训:**对"分布锐化的意外产物"(而非策略性选择),奖励惩罚只能定价、
不能防止——invalid 全程被罚 0.2 却照样增长,是同一定理的另一面**。

**结构性修复:自由 token 梯度屏蔽**(--grad_mask_free)。策略梯度只落在
动作语义 token + EOS 上;think/裸文本的 logprob 不进 loss——通胀的引擎
(广播)被整体移除,而非与之博弈。相对 ban(采样禁词)的优势:ban 只堵
一个句法,压力改道(parser 容忍裸散文,实证见 §9 表);屏蔽按 parser 的
语义分界一刀切,无处改道。c_tok 保留在 R 侧(轨迹间比较的量纲一致性),
与屏蔽正交。

预注册预测:屏蔽启用后 ftk 应长期停留在 SFT 锚定水平(0.2–1,无梯度推
力,仅表示漂移的二阶残余),若仍增长则机理判断有误,须回炉。

**§9 补充(2026-08-09,五启停车协议)**:屏蔽启用后,停车规则改为维度
敏感 + 宽限期:任务奖励维度的下滑(em/ans 掉、ftk/inv 健康)不干预——
探索谷享有到 ep100 的完整耐心;风格维度的棘轮签名(ftk≥5 或 inv≥0.15,
按 5-ep 窗)触发后给 **连续 10 窗(50 ep)自愈宽限**,任何一窗回线即清零
重计,10 窗不愈才停。设计意图:把"先变坏再变好"从直觉升格为可证伪实验
——若在无广播推力(已屏蔽)的条件下棘轮仍现且 50 ep 不愈,吸收态结论
即成铁案;若自愈,得到 RL 自我修复的正面证据。

**§9 终章(2026-08-09,五启 10 窗宽限实验完整曲线)**:全屏蔽(mode 1)
下 ftk 逐窗 0.4→1.7→2.6→8.3→30.9→67.3→211→307→376→448(顶格),
inv 同步 0→0.01→0.08→0.33→0.87→0.94→0.95→0.96→1.00→1.00,50 ep
单调至完全吸收、零恢复。ep60 起幸存者 em 也崩(0.87→0.62→0.44→0)——
错误归因的二次伤害(混杂轨迹的负优势砸在健康 turn 上)。两个定论:
(1) 生成式 RL 的格式退化是吸收态,"先变坏再变好"不适用于风格维度;
(2) 全屏蔽在拆通胀引擎的同时缴械了纠偏通道(invalid 内容零负梯度),
比不屏蔽崩得更快——**梯度屏蔽必须非对称**(mode 2:自由 token 仅在
优势>0 时屏蔽),这是本节五次失败换来的最终设计。

**§9 收官(2026-08-10,七启双档 K3 锚)**:锚修复(K1→K3 + 动作 0.02/
自由 0.5 双档,屏蔽退役)后,ftk 在历届全部起飞确认点(ep20/25/30)钉死
0.1–0.3——历届同期 1.7–25.9。关键区别是动力学消失而非水平降低:漂移
张力被吸收在分布层(kl_act 0.002→0.034 缓升 = 正常学习信号),token 层
零通胀。结论闭环:六次崩塌的共同根因是 K1 伪锚(无定向拉力),五种梯度
通道干预(计价×2/屏蔽×2/非对称)只能改变通胀的表现水平、从未触及增速,
因为它们都在与一个不存在约束的策略空间博弈。**方法论教训:当多种机制
级干预对某病理的动力学(增速/起飞点)完全无效时,病根在约束缺失层,
不在激励层。**

**§9 治愈确认(2026-08-10,七启 ep50 落笔)**:双档 K3 锚(动作 0.02 /
自由 0.5,屏蔽退役)下,ftk 全程 0.0–0.4 贴地穿越历史崩塌区(ep20–40,
历届此区间 ftk 已达 8–68),inv 连续 50 ep 为零;kl_act 在 0.05–0.06 附近
震荡(信任域均衡,非单调漂移),kl_free 恒零。反向确认根因:六次崩塌
源于锚失效(K1 无定向拉力),而非任何自由 token 梯度通道机制——五种
通道干预(计价×2/屏蔽×2/无)增速全同即其证据。最终设计一句话:
**外部信息 c_r 计价、内部计算 c_tok 计价、动作分布弱锚、自由分布强锚**;
学习发生在动作通道,风格钉死在 SFT 流形。
