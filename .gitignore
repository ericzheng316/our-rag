# 大数据目录
data/
models/
node-v22.14.0-linux-x64/
node.tar.xz
logs/
*.log

# conda/pip 环境
.conda/
.local/
.cache/
.npm/

# Claude 配置
.claude/
.claude.json

# Jupyter
.jupyter/
.ipython/
.ipynb_checkpoints/

# Python cache
__pycache__/
*.pyc
*.pyo
*.bin
*.pt
*.safetensors

# API Keys — NEVER commit
nim_keys.json

# 第三方工具（嵌套 git repo，不提交）
rag/tool/ircot/

# 其他
.triton/
.bash_history
.bashrc
.config/

# macOS
.DS_Store

# rag/{data,log,model} scaffold — keep the empty directory path (via .gitkeep),
# ignore everything actually downloaded/generated into it (datasets, run logs,
# model weights) so those never get committed.
!rag/data/
rag/data/*
!rag/data/.gitkeep
rag/log/*
!rag/log/.gitkeep
rag/model/*
!rag/model/.gitkeep

# Runtime-generated, per-machine (written by 02_start_retriever.sh /
# 03_start_split_server.sh with that machine's actual IP) — never a source
# file, shouldn't be committed.
run_scripts/.env_retriever
