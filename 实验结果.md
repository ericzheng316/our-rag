# 实验结果记录

数据集：HotpotQA dev，7405 条，num_search=5，docs=10

---

## 基准线

### Distractor baseline（2026-04-23，ICRN H200）
> 脚本：06_run_full_baseline.sh

| EM_proc | F1_proc | Judge | avg_docs |
|---------|---------|-------|----------|
| 58.0% | 74.9% | 81.6% | 11.21 |

### Distractor belief doc-filter th=0.70（2026-04-23，ICRN H200）
> 脚本：07_run_full_belief.sh

| EM_proc | F1_proc | Judge | avg_docs |
|---------|---------|-------|----------|
| 58.0% | 74.9% | 81.6% | 11.21 |

**结论：** Belief 在 distractor 模式下无效。原因：gold doc 与 distractor doc 的 E5 相似度几乎相同，`ret_quality` 无法区分，阈值从未触发。

---

### Real retrieval baseline（2026-04-24，H100）
> 脚本：09_run_real_baseline.sh → 11_calc_metrics_real.sh

num_search 分档结果：

| num_search | EM_proc | F1_proc | Judge | avg_docs |
|------------|---------|---------|-------|----------|
| 1 | 4.4% | 5.5% | 6.5% | 8.92 |
| 2 | 18.9% | 25.7% | 28.1% | 14.59 |
| 3 | 41.4% | 55.6% | 60.2% | 15.42 |
| 4 | 43.0% | 57.7% | 62.5% | 15.71 |
| **5** | **43.2%** | **58.1%** | **62.8%** | **15.87** |

---

## Belief 实验

### Real retrieval + BeliefState early stopping v1（2026-04-24，H100）
> 脚本：10_run_real_belief.sh（--belief_threshold 0.70，--belief_saturation_threshold 0.45）→ 11_calc_metrics_real.sh

| num_search | EM_proc | F1_proc | Judge | avg_docs |
|------------|---------|---------|-------|----------|
| 1 | 4.4% | 5.5% | 6.5% | 8.92 |
| 2 | 19.0% | 25.7% | 28.2% | 14.59 |
| 3 | 33.8% | 46.6% | 50.7% | 15.76 |
| 4 | 34.9% | 48.4% | 52.6% | 16.23 |
| **5** | **35.1%** | **48.6%** | **52.9%** | **16.53** |

**对比 Real baseline（num_search=5）：EM -8.1pt，F1 -9.5pt，Judge -9.9pt，avg_docs +0.66（更多轮次）**

#### 诊断（三个根本原因）

1. **主因 −8pt：retriever 未返回 score**
   - `retrive_server.py` 调用 `batch_search()` 默认 `return_score=False`
   - inference_new.py 中 `d.get('score', 0.72)` 全部 fallback 到 0.72
   - `_rerank_pool_by_score` 排序失效，cross-turn 证据池按插入顺序返回，模型拿到错误文档

2. **+62 extra fail：max_turns 多给 1 轮**
   - `extra = round(difficulty × 4)`，difficulty=0.25 → extra=1 → max_turns=6（baseline=5）
   - 6776/7405 样本拿到 max_turns=6
   - force_answer 在第 6 轮触发，95.8% 失败（118 条 force_answer，113 条 fail）

3. **Early stopping 从未触发**
   - `belief_saturation_threshold=0.45`，实际 ret_quality ≈ 0.65，永远 > 阈值
   - 饱和检测完全失效

#### 待修复（下一版）
- [x] retrive_server.py：`return_score=True`，score 注入 doc dict
- [x] max_turns 公式：`extra = max(0, int(difficulty*4) - 1)`，difficulty=0.25 → extra=0
- [ ] 重新校准 belief_saturation_threshold 或换更有区分力的信号

---

### Real retrieval + BeliefState early stopping v2（2026-04-24，H100）
> 修复：retriever 返回真实 score；max_turns 公式修正

| num_search | EM_proc | F1_proc | Judge | avg_docs |
|------------|---------|---------|-------|----------|
| 1 | 4.4% | 5.5% | 6.5% | 8.92 |
| 2 | 19.0% | 25.8% | 28.4% | 14.56 |
| 3 | 40.5% | 54.3% | 59.1% | 15.41 |
| 4 | 41.8% | 56.2% | 61.1% | 15.71 |
| **5** | **41.9%** | **56.5%** | **61.3%** | **15.90** |

**对比 Real baseline（num_search=5）：EM -1.3pt，F1 -1.6pt，Judge -1.5pt，avg_docs ≈ 持平**

修复 score 后大幅收回 v1 损失（EM: -8.1pt → -1.3pt）。残余差距来源：
- cross-turn reranking 将全局 pool 混入当前 query，有干扰
- early stopping 仍未触发（ret_quality ≈ 0.65 > saturation_threshold=0.45）

---

### Belief no_rerank ablation（2026-04-25，H100）
> 关闭 cross-turn reranking，belief 其余机制保持

| num_search | EM_proc | F1_proc | Judge | avg_docs |
|------------|---------|---------|-------|----------|
| **5** | **43.5%** | **58.1%** | **62.9%** | **15.87** |

**结论：no_rerank ≈ baseline（±0.3pt）。Belief 的 budget 和 early stopping 均未触发，唯一改变行为的是 reranking（有害）。关掉后 belief 对推理零干预。**

---

### Real retrieval + BeliefState v3（Tasks 1-4 full）（2026-04-25，H100）
> 脚本：10c_run_real_belief_v3.sh → 11c_calc_metrics_v3.sh

| num_search | EM_proc | F1_proc | Judge | avg_docs |
|------------|---------|---------|-------|----------|
| **5** | **43.1%** | **57.6%** | **62.5%** | **15.59** |

**对比 no_rerank（num_search=5）：EM -0.4pt，F1 -0.5pt，avg_docs -0.28（略少）**

#### 诊断（三个根本原因）

1. **pool_docs None 判断 bug（主因）**
   - `if pool_docs is not None:` 当 pool_docs=[] 时为 True → 走 novelty 路
   - turn 0 pool 为空，novelty=1.0 → β更新：α:2→3, β:1→1 → ret_quality 锁死 0.750（零方差）
   - deficit = 1-0.75 = 0.25 → round(exp(0.375)) = 1 → 全部样本 extra=1 → max_turns=6（重现 v1 bug）
   - **修复：`if pool_docs:`（truthiness 判断）**

2. **budget 公式在 deficit=0 时给 extra=1**
   - 原公式 `round(exp(1.5*d))` 在 d=0 时给 round(1.0)=1，即使 retrieval 完美也多给一轮
   - **修复：`round(exp(1.5*d)) - 1`，deficit=0 → extra=0**

3. **Condition A 阈值 0.85 过于激进**
   - 776 次误停（占 10.5%），触发值集中在 0.853（barely above 0.85）
   - 这些样本大多还需要更多检索 → 提前终止 → 轻微损失
   - **修复：阈值 0.85 → 0.92**

---

### Real retrieval + BeliefState v3b（三 bug 修复 + no_rerank）（2026-04-25，H100）
> 脚本：10d_run_real_belief_v3b.sh → 11d_calc_metrics_v3b.sh
> 修复：pool_docs None 判断；budget -1 修正；Condition A 0.85→0.92；禁用 RRF

*（待跑）*
